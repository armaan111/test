pipeline {
    agent any

    stages {
        stage('clone repo') {
            steps {
            sh "export AWS_DEFAULT_REGION=us-east-1"
            sh "aws cloudformation create-stack --stack-name ec2stack --template-body file://ec2111.yaml --region us-east-1"
           
      
            }
        }
    }
}

pipeline {
    agent any

    stages {
        stage('clone repo') {
            steps {
            sh "export AWS_DEFAULT_REGION=ap-south-1"
            sh "aws cloudformation create-stack --stack-name atomstack --template-body file://ec2.yaml --region ap-south-1"
           
      
            }
        }
    }
}

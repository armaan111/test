 - name: I am an adult or child
   hosts: localhost
   vars:
     age: 25
   tasks:

      - command: echo "I am a child"
        when: age < 18

      - command: echo " I am an Adult"
        when: age >=18 
